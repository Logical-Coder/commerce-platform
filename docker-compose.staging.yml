services:
  mysql:
    image: mysql:8.4
    container_name: commerce-mysql-staging
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
    volumes:
      - commerce_mysql_staging:/var/lib/mysql
      - ./docker/my_sql/init:/docker-entrypoint-initdb.d
    networks:
      - commerce-staging-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  redis:
    image: redis:7
    container_name: commerce-redis-staging
    restart: unless-stopped
    volumes:
      - commerce_redis_staging:/data
    networks:
      - commerce-staging-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      timeout: 3s
      retries: 5

  identity-service:
    image: ghcr.io/logical-coder/identity-service:latest
    container_name: identity-service-staging
    restart: unless-stopped
    env_file:
      - ./identity-service/.env
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      REDIS_HOST: redis
      REDIS_PORT: 6379
      USER_RATE_LIMIT: ${USER_RATE_LIMIT:-50/min}
    command: >
      sh -c "
      sh scripts/wait_for_db.sh &&
      python manage.py migrate &&
      gunicorn identity_service.wsgi:application --bind 0.0.0.0:8000 --workers 1
      "
    ports:
      - "8001:8000"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - commerce-staging-network

  product-service:
    image: ghcr.io/logical-coder/product-service:latest
    container_name: product-service-staging
    restart: unless-stopped
    env_file:
      - ./product-service/.env
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      REDIS_HOST: redis
      REDIS_PORT: 6379
      USER_RATE_LIMIT: ${USER_RATE_LIMIT:-50/min}
    command: >
      sh -c "
      sh scripts/wait_for_db.sh &&
      python manage.py migrate &&
      gunicorn product_service.wsgi:application --bind 0.0.0.0:8000 --workers 1
      "
    ports:
      - "8003:8000"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - commerce-staging-network

  gateway-service:
    image: ghcr.io/logical-coder/gateway-service:latest
    container_name: gateway-service-staging
    restart: unless-stopped
    environment:
      ENVIRONMENT: staging
      DEBUG: "False"
      IDENTITY_SERVICE_URL: http://identity-service:8000
      PRODUCT_SERVICE_URL: http://product-service:8000
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-localhost,127.0.0.1}
      USER_RATE_LIMIT: ${USER_RATE_LIMIT:-50/min}
    command: gunicorn gateway_service.wsgi:application --bind 0.0.0.0:8000 --workers 1
    ports:
      - "8081:8000"
    depends_on:
      - identity-service
      - product-service
    networks:
      - commerce-staging-network

volumes:
  commerce_mysql_staging:
  commerce_redis_staging:

networks:
  commerce-staging-network:
    driver: bridge
