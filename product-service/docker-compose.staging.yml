services:
  web:
    image: ghcr.io/logical-coder/product-service:latest
    container_name: product-service-web-staging
    command: >
      sh -c "
      sh scripts/wait_for_db.sh &&
      python manage.py migrate &&
      gunicorn product_service.wsgi:application --bind 0.0.0.0:8000 --workers 1
      "
    restart: unless-stopped
    env_file:
      - .env
    environment:
      ENVIRONMENT: docker
      DB_HOST: db
      DB_PORT: 3306
      REDIS_HOST: redis
      REDIS_PORT: 6379
    ports:
      - "8003:8000"
    depends_on:
      - db
      - redis
    networks:
      - product-staging-network

  db:
    image: mysql:8.4
    container_name: product-service-mysql-staging
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: commerce_product_db
    volumes:
      - product_mysql_data_staging:/var/lib/mysql
    networks:
      - product-staging-network

  redis:
    image: redis:7
    container_name: product-service-redis-staging
    restart: unless-stopped
    volumes:
      - product_redis_data_staging:/data
    networks:
      - product-staging-network

volumes:
  product_mysql_data_staging:
  product_redis_data_staging:

networks:
  product-staging-network:
    driver: bridge